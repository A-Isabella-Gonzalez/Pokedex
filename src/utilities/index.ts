export function decimetersToInches(dm: number): number {
  const inchesPerDecimeter = 3.93701;
  return dm * inchesPerDecimeter;
}

export function hectogramsToPounds(hg: number): number {
  const poundsPerHectogram = 0.220462;
  return hg * poundsPerHectogram;
}
